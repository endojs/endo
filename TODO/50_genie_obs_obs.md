
- [ ] analyze the genie observer and reflector as integrated in `packages/genie/dev-repl.js`
  - the TODOs between lines 678 and 696

1. [ ] write a design plan here for how to implement observer and reflector
   agent event streaming and print it from dev-repl similar to primary agent 

2. [ ] design so that we can block the special `.observe` and `.reflect`
   commands until observer (resp reflector) are done

