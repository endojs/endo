---
'@endo/agentry': major
---

Rename the public `@endo/agentry/execute` subpath to `@endo/agentry/code-mode` and rename the code-mode operation from `execute` to `evaluate`.
Update imports and custom evaluator options to use the new names.
