Work on `packages/daemon/src/mount.js`:
- [x] eject its `mountHelp` into `help.md` and `help-text.js` like the other daemon help sections
- [x] simplify its exo's `help()` method around line 184 by using `makeHelp`
