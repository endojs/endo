# Change Log

## 1.0.0 (Unreleased)

Initial release.
A minimal, portable tar archive reader for the entries that
`git archive --format=tar` emits (regular files, directories, symlinks, and
pax extended headers).
