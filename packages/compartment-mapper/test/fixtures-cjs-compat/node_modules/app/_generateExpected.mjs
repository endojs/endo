// run me with:
//  node ./test/fixtures-cjs-compat/node_modules/app/test.mjs
import * as interops from "interops"
import * as moduleinterops from "app/moduleinterops"

console.log(
    stableOrderJson(interops)
)
console.log(
    stableOrderJson(moduleinterops)
)


function stableOrderJson(obj) {
    return JSON.stringify(obj, Object.keys(obj).sort());
}