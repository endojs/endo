// Fixture for lexer/parser divergence on syntactically-invalid object literals.
//
// `{ a = 5, b }` is valid only as a destructuring *pattern*, never as an object
// literal *expression*. Both Babel and any real JS engine reject it.
//
//  - The `babel` CJS parser (`CjsModuleSource`) does a full parse and throws a
//    SyntaxError at *analysis* time.
//  - The `default` CJS parser (the character-level lexer) does not fully parse,
//    so it "succeeds" at analysis and reports export names — but the module can
//    never *execute*, because `compartment.evaluate()` of this source hits the
//    same SyntaxError the lexer skipped over.
//
// The point of the fixture is to observe *where* each parser fails, not to make
// it load successfully (it cannot).
module.exports = { a = 5, b };
