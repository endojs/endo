function nothing() {
  const require = () => {};
  require('spam', 'asifitsdoingsomething');
  // require('/spam'); // TODO: prevent this from causing errors
  require('./spam');
  require(Mime);
  require({ stuff: 1 });
  require('spam');
}

// TODO: fix the export - .default should not be here
function notMuch(item) {
  const isOk = require('./is-ok').isOk || require('./is-ok').default.isOk;
  return { isOk: (item.isOk || item.default.isOk) && isOk };
}
module.exports = notMuch(require('./is-ok'));
