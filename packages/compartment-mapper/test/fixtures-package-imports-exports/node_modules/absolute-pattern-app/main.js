// This import resolves through "absolute-pattern-lib" which has an
// exports pattern that maps "./smuggle/*.js" to "/etc/*.js".
// The compartment-mapper must reject this because the resolved module
// specifier is an absolute path, not a relative one.
import { secret } from 'absolute-pattern-lib/smuggle/passwd.js';

export { secret };
