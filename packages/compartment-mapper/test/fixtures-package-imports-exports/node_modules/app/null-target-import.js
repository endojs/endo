// This import should fail because patterns-lib exports
// "./features/secret/*.js": null, which excludes this path
// even though "./features/*.js" would otherwise match.
import { secret } from 'patterns-lib/features/secret/data.js';

export { secret };
