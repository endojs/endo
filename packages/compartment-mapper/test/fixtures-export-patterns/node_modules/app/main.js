// Entry point that imports via wildcard patterns
// * matches "foo/y/bar/z" across / separators (Node.js semantics)
import { value } from 'app/x/foo/y/bar/z.js';
import { helper } from '#internal/util.js';

export { value, helper };
