// Node's pattern key comparison prefers "./tie/*.js" over "./tie/*"
// because both share the same prefix length and the longer full key wins.
import { tieBreak } from 'patterns-lib/tie/bar.js';

export { tieBreak };
