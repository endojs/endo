// An object (rather than a primitive) so that reference-identity checks in
// the test suite actually prove two specifiers resolved to the same module
// instance, rather than merely to equal-by-value results.
export default { foo: 'foo' };
