export const getFoo = async () => {
  const foo = await import('./foo.js');
  return foo.default;
};

// Specifier is opaque to the parser at bundle/archive time — used by
// runtime-computed-specifier tests.
export const getDynamic = async specifier => {
  const ns = await import(specifier);
  return ns.default;
};
