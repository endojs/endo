// dynamic import not supported yet
// export const clarke = import('clarke');

// export references to potentially dangerous values
export const myEval = eval;
export const myGlobalThis = globalThis;
export const mySecret = globalThis.secret;

// write to local globalThis
try {
  globalThis.pollution = 'stinky';
} catch (_error) {
  // ignore failure to write to module's globalThis
}