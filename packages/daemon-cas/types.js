// @ts-check
// This module exists only to provide a JavaScript counterpart for types.d.ts.
// All types are defined in types.d.ts and re-exported from here for tooling
// compatibility (the `@import` JSDoc tag resolves `./types.js` to the
// matching `.d.ts` declaration file).
