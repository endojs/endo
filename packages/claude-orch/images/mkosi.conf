# Root mkosi config. Per-architecture rootfs builds are driven by
# scripts/build-image.sh; the actual content lives in mkosi.conf.d/.

[Distribution]
Distribution=alpine
Release=3.20

[Output]
Format=disk
ImageId=claude-sandbox
Output=rootfs

[Content]
Bootable=no
