/**
 * @deprecated Import these types from `@endo/eventual-send` directly.
 * `@endo/far` is a plain re-exporter (endojs/endo-but-for-bots#543): importing a
 * name through it rather than from the package that originally exports it is
 * discouraged, and this re-export is slated for removal in a future major
 * version.
 */
export { FarRef, ERef, EOnly, EReturn, EResult } from '@endo/eventual-send';
