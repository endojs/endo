// @ts-check

// Surface module: alias for the package's public adapter at the
// `@endo/exo-unzip/unzip.js` path. Identical to the package-root
// import (`@endo/exo-unzip`); both are kept so callers may pick the
// path that suits their convention.

export { unzip } from './src/unzip.js';
