# OpenAI API (uses the same OpenAI-compatible provider as llama.cpp)
# Get an API key from https://platform.openai.com/api-keys
# LAL_MODEL: e.g. gpt-4o, gpt-4o-mini, gpt-4-turbo (see https://platform.openai.com/docs/models)

export LAL_HOST=https://api.openai.com/v1
export LAL_MODEL=gpt-4o
export LAL_AUTH_TOKEN=
