// @ts-check

// Planned adapter shape only.
// An MCP consumer will map a ToolRecord's `name`, `description`, `parameters`,
// and `invoke` to an MCP tool without adding MCP runtime dependencies here.
export {};
