// @ts-check

// Surface module: alias for the package's public adapter at the
// `@endo/exo-zip/zip.js` path. Identical to the package-root
// import (`@endo/exo-zip`); both are kept so callers may pick the
// path that suits their convention.

export { zip } from './src/zip.js';
